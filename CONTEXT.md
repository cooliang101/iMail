# iMail

iMail 将多邮箱邮件能力集中在服务实例中。桌面客户端既可使用当前用户的本地实例，也可与 Web 客户端共同连接远程实例。

## Language

**iMail 服务**:
拥有邮箱凭据、邮件数据、同步任务、HTTP API、Gateway 与 MCP 能力的服务实例；本地与远程部署具有相同的领域职责。
_Avoid_: 桌面后端

**iMail 客户端**:
连接一个 iMail 服务实例的 Web 或桌面界面；只保存客户端偏好和服务选择，不拥有邮箱服务端状态。
_Avoid_: 邮件服务、桌面后端

**本地服务模式**:
桌面客户端使用当前设备上的 iMail 服务实例，数据只属于该本地实例。
_Avoid_: 系统服务模式、独立安装模式

**本地守护服务**:
随桌面应用分发、由桌面应用管理并以当前操作系统用户身份持续运行的本地 iMail 服务实例。
_Avoid_: 系统服务、桌面子进程

**远程服务模式**:
桌面或 Web 客户端共同连接用户部署的远程 iMail 服务实例，以共享该实例中的数据。
_Avoid_: 云同步、本地远程同步

**服务地址**:
iMail 客户端用于访问一个 iMail 服务实例的 HTTP 或 HTTPS 基地址。
_Avoid_: sidecar 地址、固定端口

**当前用户邮箱数据**:
当前登录 iMail 用户拥有的邮箱账户与授权、邮件缓存、草稿、联系人、开发者令牌和同步状态；不包含 iMail 登录账号、服务文件、主密钥或其他用户的数据。
_Avoid_: 本地全部数据、实例数据

**邮箱授权导出**:
由登录后的“隐私与数据”界面创建、使用独立密码加密的当前用户邮箱连接配置与凭据文件；不包含邮件内容，也不属于 Gateway 或 MCP 能力。
_Avoid_: 邮件备份、账号数据导出

**变化唤醒**:
邮箱服务通过持久连接提示收件箱可能发生变化，iMail 据此立即安排一次增量拉取；提示本身不是邮件数据，也不保证覆盖断线期间或未监听文件夹的变化。
_Avoid_: 实时同步流、邮件推送数据

**一致性校准**:
iMail 在服务启动、持久连接恢复和低频后台检查时执行的增量核对，用于补齐遗漏变化并校准非收件箱文件夹；它是系统可靠性机制，不是用户选择的同步频率。
_Avoid_: 定时同步、轮询策略

**自动同步设置**:
用户决定某个邮箱是否接受变化唤醒与一致性校准，以及需要保持一致的文件夹范围和失败通知；不包含内部校准周期。
_Avoid_: 同步策略、同步频率
