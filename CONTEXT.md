# iMail

iMail 将多邮箱邮件能力集中在 Rust 领域服务中。Windows 桌面端可在 Tauri 进程内直接嵌入该服务，也可与 Web 客户端共同连接远程 Rust 服务。

## Language

**iMail 服务**:
拥有邮箱凭据、邮件数据和同步任务的 Rust 领域服务；Tauri 嵌入模式与远程 HTTP 模式复用相同的领域职责。
_Avoid_: 桌面后端

**iMail 客户端**:
使用嵌入式 Rust 服务或连接远程 Rust 服务的 Web/桌面界面；只保存客户端偏好和服务选择，不拥有邮箱领域状态。
_Avoid_: 邮件服务、桌面后端

**本地服务模式**:
Windows 桌面客户端通过类型化 Tauri command/event 直接调用进程内 Rust 服务；不开放常驻业务 HTTP 端口，数据只属于该本地实例。
_Avoid_: 系统服务模式、独立安装模式

**嵌入式服务**:
静态链接进 Windows Tauri 应用、随托盘进程运行的 Rust 服务；窗口隐藏后继续同步，用户显式退出后停止。
_Avoid_: Node 守护服务、sidecar、本地 HTTP 服务

**远程服务模式**:
桌面或 Web 客户端通过显式启用的 Rust HTTP Adapter 连接用户部署的远程 iMail 服务实例，以共享该实例中的数据。
_Avoid_: 云同步、本地远程同步

**服务地址**:
iMail 客户端用于访问远程服务实例的 HTTP 或 HTTPS 基地址；本地嵌入模式没有服务地址。
_Avoid_: sidecar 地址、固定端口

**当前用户邮箱数据**:
当前登录 iMail 用户拥有的邮箱账户与授权、邮件缓存、草稿、联系人、开发者令牌和同步状态；不包含 iMail 登录账号、服务文件、主密钥或其他用户的数据。
_Avoid_: 本地全部数据、实例数据

**邮箱授权导出**:
由登录后的“隐私与数据”界面创建、使用独立密码加密的当前用户邮箱连接配置与凭据文件；不包含邮件内容，也不属于 Gateway 或 MCP 能力。
_Avoid_: 邮件备份、账号数据导出

**变化唤醒**:
邮箱服务通过持久连接提示收件箱可能发生变化，iMail 据此立即安排一次增量拉取；提示本身不是邮件数据，也不保证覆盖断线期间或未监听文件夹的变化。
_Avoid_: 实时同步流、邮件推送数据

**一致性校准**:
iMail 在服务启动、持久连接恢复和低频后台检查时执行的增量核对，用于补齐遗漏变化并校准非收件箱文件夹；它是系统可靠性机制，不是用户选择的同步频率。
_Avoid_: 定时同步、轮询策略

**自动同步设置**:
用户决定某个邮箱是否接受变化唤醒与一致性校准，以及需要保持一致的文件夹范围和失败通知；不包含内部校准周期。
_Avoid_: 同步策略、同步频率

**附件预览会话**:
iMail 为当前用户按需读取单个邮件附件后创建的短期、受控查看上下文；用于展示支持的图片、PDF、视频、纯文本或 ZIP 内容，不是附件下载、原始附件缓存或系统程序打开。
_Avoid_: 附件解压目录、附件副本、在线文档服务

**附件本地缓存**:
iMail 首次从邮箱服务器成功读取附件后保存的用户隔离副本，供后续下载和预览复用；具有容量、过期与完整性校验边界，并随用户邮箱数据一起清除。
_Avoid_: 邮件备份、永久附件库、预览会话
