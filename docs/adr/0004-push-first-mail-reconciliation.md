---
status: accepted
---

# 邮箱同步采用变化唤醒优先与后台一致性校准

iMail 使用 IMAP IDLE 等邮箱侧变化提示立即唤醒增量拉取，并把启动、连接恢复和低频后台检查统一视为一致性校准。校准周期属于服务端可靠性参数，不开放为用户或 MCP 可调策略；用户只控制自动同步开关、文件夹范围和失败通知。这样可以避免正常连接下的高频重复拉取，同时保留对不支持 IDLE、断线漏报及非收件箱变化的最终一致性保障。
