{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the bundled iMail client can use narrowly scoped desktop integrations.",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "opener:allow-default-urls",
    "dialog:allow-save",
    "notification:default"
  ]
}
